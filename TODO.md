- change alignment in web interface so that it is not center-aligned and it always takes up as much of the screen as possible
- add a status page ([url]/status) with:
	- git commit
	- uptime (app, not system)
	- current users
	- current CPU usage
	- current tokens/s of all models
	- database size
	- used space in /data
- fix: doesn't save data when the application crashes
- fix: users (not admins) still have the button to delete shown in the web interface
- minor: remove legacy .env token naming
